#include <iostream>
#include <vector>
#include <string>
#include <set>
#include <algorithm>
using namespace std;

void solve() {

}

vector<pair<int,int>> points;

int main() {
    //int n, k;
    //cin >> n >> k;

    /*
    while(n--) {
        solve(k);
    }
    */

    int n;
    cin >> n;
    vector<int> circle(n);
    
    for (int i = 0; i < n; i++) {
        cin >> circle[i];
    }

    int mindiff = 100000;
    pair<int, int> positions;
    for (int i = 0; i < n - 1; i++) {
        if (abs(circle[i] - circle[i + 1]) < mindiff) {
            mindiff = abs(circle[i] - circle[i + 1]);
            positions = make_pair(i, i + 1);
        }
    }

    if (abs(circle[0] - circle[circle.size() - 1]) < mindiff) {
        mindiff = abs(circle[0] - circle[circle.size() - 1]);
        positions = make_pair(0, circle.size() - 1);
    }

    cout << positions.first + 1 << " " << positions.second + 1 << endl;

    return 0;
}

/*
Sample Input:

*/