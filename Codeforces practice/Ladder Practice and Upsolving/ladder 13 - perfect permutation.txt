#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
using namespace std;

void solve() {

}

int main() {
    //int n, k;
    //cin >> n >> k;
    
    /*
    while(n--) {
        solve(k);
    }
    */
    
    int n;
    cin >> n;
    vector<int> perfect(100);
    
    perfect[0] = 2;
    perfect[1] = 1;
    for (int i = 2; i < 100; i += 2) {
        perfect[i    ] = perfect[i - 2] + 2;
        perfect[i + 1] = perfect[i - 1] + 2;
    }

    if (n % 2) {
        cout << -1 << endl;
    }
    else {
        for (int i = 0; i < n; i++) {
            cout << perfect[i] << " " << endl;
        }
    }

    return 0;
}

/*
Sample Input:

*/