#include <iostream>
#include <vector>
#include <string>
#include <set>
#include <algorithm>
using namespace std;

void solve() {

}

int main() {
    //int n, k;
    //cin >> n >> k;
    
    /*
    while(n--) {
        solve(k);
    }
    */
    
    int arr[26] = { 0 };
    string s;
    cin >> s;

    for (int i = 0; i < s.size(); i++) {
        arr[s[i] - 'a']++;
    }

    int distinct = 0;
    for (int i = 0; i < 26; i++) {
        if (arr[i] != 0) {
            distinct++;
        }
    }

    if (distinct % 2 == 0) {
        cout << "CHAT WITH HER!" << endl;
    }
    else
        cout << "IGNORE HIM!" << endl;

    return 0;
}

/*
Sample Input:

*/