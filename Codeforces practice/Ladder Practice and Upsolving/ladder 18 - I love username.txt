#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
using namespace std;

void solve() {

}

int main() {
    //int n, k;
    //cin >> n >> k;
    
    /*
    while(n--) {
        solve(k);
    }
    */
    
    int n;
    cin >> n;
    
    int max = -1;
    int min = 1000000;

    int ans = 0;
    for (int i = 0; i < n; i++) {
        int score;
        cin >> score;

        if (max < score) {
            max = score;
            ans++;
        }
        if (min > score) {
            min = score;
            ans++;
        }
    }

    cout << ans-2 << endl;
    return 0;
}

/*
Sample Input:

*/