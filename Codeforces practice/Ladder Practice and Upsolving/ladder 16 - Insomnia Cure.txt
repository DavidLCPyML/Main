#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
using namespace std;

void solve() {

}

int main() {
    //int n, k;
    //cin >> n >> k;
    
    /*
    while(n--) {
        solve(k);
    }
    */
    
    int k, l, m, n, d;
    cin >> k >> l >> m >> n >> d;

    vector<int> nums(d, 1);

    for (int i = k-1; i < d; i += k) {
        //cout << nums[i - 1] << endl;
        nums[i] = 0;
    }
    for (int i = l-1; i < d; i += l) {
        //cout << nums[i - 1] << endl;
        nums[i] = 0;
    }
    for (int i = m-1; i < d; i += m) {
        //cout << nums[i - 1] << endl;
        nums[i] = 0;
    }
    for (int i = n-1; i < d; i += n) {
        //cout << nums[i - 1] << endl;
        nums[i] = 0;
    }

    int ans = 0;
    for (int i = 0; i < d; i ++) {
        if (nums[i] == 0) {
            ans++;
        }
    }

    cout << ans << endl;

    return 0;
}

/*
Sample Input:

*/