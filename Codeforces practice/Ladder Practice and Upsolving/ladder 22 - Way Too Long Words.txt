#include <iostream>
#include <vector>
#include <string>
#include <set>
#include <algorithm>
using namespace std;

void solve() {
    string s;
    cin >> s;

    if (s.length() <= 10) {
        cout << s << endl;
        return;
    }
    else {
        cout << s[0] << s.size() - 2 << s[s.size() - 1] << endl;
    }
}

int main() {
    int t;
    cin >> t;
    
    
    while(t--) {
        solve();
    }
    

    return 0;
}

/*
Sample Input:

*/