#include <iostream>
#include <set>
#include <string>
#include <algorithm>
using namespace std;

void solve() {

}

int main() {
    //int n, k;
    //cin >> n >> k;
    
    /*
    while(n--) {
        solve(k);
    }
    */
    
    string s1, s2;
    cin >> s1 >> s2;

    string ans = s1;
    for (int i = 0; i < s1.size(); i++) {
        //cout << s1[i] - '0' << " " << s2[i] - '0' << endl;
        ans[i] = (s1[i] - '0') ^ (s2[i] - '0') + '0';
    }

    cout << ans << endl;

    return 0;
}

/*
Sample Input:

*/