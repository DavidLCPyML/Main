#include <iostream>
#include <vector>
#include <string>
#include <set>
#include <algorithm>
using namespace std;

void solve() {

}

vector<pair<int,int>> points;

int main() {
    //int n, k;
    //cin >> n >> k;

    /*
    while(n--) {
        solve(k);
    }
    */

    int n;
    cin >> n;

    vector<int> pages(7);
    for (int i = 0; i < 7; i++) {
        cin >> pages[i];
    }

    /*for (int i = 0; i < 7; i++) {
        cout << pages[i] << " ";
    }*/

    int counter = 0;
    while (n - pages[counter] > 0) {
        n -= pages[counter];

        if (counter == 6)
            counter = 0;
        else
            counter++;
    }
    cout << counter + 1 << endl;

    return 0;
}

/*
Sample Input:

*/