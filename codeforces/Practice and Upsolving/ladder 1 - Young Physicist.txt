#include <iostream>
#include <vector>
#include <tuple>
#include <algorithm>
using namespace std;

void solve() {

}

int main() {
    int n;
    cin >> n;
    int sumx = 0, sumy = 0, sumz = 0;

    while (n--) {
        int x, y, z;
        cin >> x >> y >> z;
        sumx += x;
        sumy += y;
        sumz += z;
    }
    if (sumx == sumy && sumy == sumz && sumx == 0) {
        cout << "YES" << endl;
    } else {
        cout << "NO" << endl;
    }

    return 0;
}

/*
Sample Input:

*/