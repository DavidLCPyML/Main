#include <iostream>
#include <set>
#include <string>
#include <algorithm>
using namespace std;

void solve() {

}

int main() {
    //int n, k;
    //cin >> n >> k;
    
    /*
    while(n--) {
        solve(k);
    }
    */
    
    long long n;
    cin >> n;
    string num = to_string(n);

    int luckies = 0;
    for (int i = 0; i < num.size(); i++) {
        if (num[i] == '4' || num[i] == '7') {
            luckies++;
        }
    }


    if (luckies == 4 || luckies == 7)
        cout << "YES" << endl;
    else
        cout << "NO" << endl;
    return 0;
}

/*
Sample Input:

*/