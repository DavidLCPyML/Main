#include <iostream>
#include <set>
#include <string>
#include <algorithm>
using namespace std;

void solve() {

}

int main() {
    //int n, k;
    //cin >> n >> k;
    
    /*
    while(n--) {
        solve(k);
    }
    */
    
    int n;
    cin >> n;
    string s;
    cin >> s;

    int ans = 0;
    for (int i = 1; i < s.size(); i++) {
        if (s[i - 1] == s[i]) {
            ans++;
        }
    }
    cout << ans << endl;

    return 0;
}

/*
Sample Input:

*/