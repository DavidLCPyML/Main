#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
using namespace std;

void solve() {

}

int main() {
    //int n, k;
    //cin >> n >> k;
    
    /*
    while(n--) {
        solve(k);
    }
    */
    
    int n;
    cin >> n;
    
    int max = -1;

    int curramt = 0;
    for (int i = 0; i < n; i++) {
        int on, off;
        cin >> off >> on;
        curramt = curramt + on - off;

        if (max < curramt) {
            max = curramt;
        }

        //cout << curramt << endl;
    }

    cout << max << endl;
    return 0;
}

/*
Sample Input:

*/