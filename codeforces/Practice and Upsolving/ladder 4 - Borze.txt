#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

void solve() {

}

int main() {
    //int n, k;
    //cin >> n >> k;
    
    string code;
    cin >> code;
    
    for (int i = 0; i < code.length(); i++) {
        if (code[i] == '-' && code[i + 1] == '-') {
            cout << 2;
            i=i+1;
        }
        else if (code[i] == '-' && code[i + 1] == '.') {
            cout << 1;
            i = i + 1;
        }
        else {
            cout << 0;
        }
    }
    return 0;
}

/*
Sample Input:

*/