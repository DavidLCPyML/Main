#include <iostream>
#include <vector>
#include <map>
using namespace std;

map<int, vector<int>> solve(vector<int> dimension) {
    map<int, vector<int>> m;
    vector<int> result;
    for (unsigned i = 0; i < dimension.size(); i++) {
        if (dimension[i] == 1) {
            result.push_back(1);
            m[dimension[i]] = result;
            return m;
        }

    }
}

int main() {
    int t;
    cin >> t;
    vector<int> dimensions;
    int number;
    while (t--) {
        cin >> ws;
        cin >> number;
        dimensions.push_back(number);
        number = {};
    }
    solve(dimensions);
    return 0;
}