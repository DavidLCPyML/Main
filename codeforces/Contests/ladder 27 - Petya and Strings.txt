#include <iostream>
#include <vector>
#include <string>
#include <set>
#include <algorithm>
using namespace std;

void solve() {

}

int main() {
    //int n, k;
    //cin >> n >> k;

    /*
    while(n--) {
        solve(k);
    }
    */

    string a, b;
    cin >> a >> b;

    for (int i = 0; i < a.size(); i++) {
        a[i] = toupper(a[i]);
        b[i] = toupper(b[i]);
    }
    //cout << a << endl << b << endl;

    if (a < b)
        cout << -1 << endl;
    else if (a > b)
        cout << 1 << endl;
    else
        cout << 0 << endl;

    return 0;
}

/*
Sample Input:

*/